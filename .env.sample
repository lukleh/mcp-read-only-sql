# MCP Read-Only SQL Server - Environment Variables Sample
# Copy to .env and fill with your actual values

# =============================================================================
# Database Passwords
# =============================================================================
# Format: DB_PASSWORD_<CONNECTION_NAME>
# Connection names are uppercase with hyphens replaced by underscores

# For connections.yaml.sample examples:
DB_PASSWORD_BASIC_POSTGRES=your_password_here
DB_PASSWORD_BASIC_CLICKHOUSE=your_password_here
DB_PASSWORD_POSTGRES_CLUSTER=your_password_here
DB_PASSWORD_CLICKHOUSE_CLUSTER=your_password_here
DB_PASSWORD_RESTRICTED_POSTGRES=your_password_here
DB_PASSWORD_LIMITED_ANALYTICS=your_password_here
DB_PASSWORD_PUBLIC_READONLY=your_password_here
DB_PASSWORD_LEGACY_POSTGRES_CLI=your_password_here
DB_PASSWORD_CLICKHOUSE_CLI=your_password_here
DB_PASSWORD_FULL_FEATURED=your_password_here
DB_PASSWORD_LOCAL_POSTGRES=your_password_here
DB_PASSWORD_DOCKER_POSTGRES=your_password_here

# For test connections (connections-test.yaml):
DB_PASSWORD_TEST_POSTGRES=testpass
DB_PASSWORD_TEST_CLICKHOUSE=testpass
DB_PASSWORD_TEST_CLICKHOUSE_CLUSTER=testpass

# =============================================================================
# SSH Passwords (if not using key-based auth)
# =============================================================================
# Format: SSH_PASSWORD_<CONNECTION_NAME>
# Note: SSH usernames are configured in connections.yaml under ssh_tunnel.user
SSH_PASSWORD_REMOTE_POSTGRES_SSH=ssh_password_here
SSH_PASSWORD_REMOTE_CLICKHOUSE_SSH=ssh_password_here
SSH_PASSWORD_FULL_FEATURED=ssh_password_here

# =============================================================================
# Notes
# =============================================================================
# 1. Never commit .env file with real passwords
# 2. Connection names are converted: "my-connection" → "MY_CONNECTION"
# 3. SSH passwords only needed if not using private key authentication
# 4. Test passwords can be committed (they're for local Docker containers)
